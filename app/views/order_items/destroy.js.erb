$('#order-item-<%= @order_item.id %>').remove();
$('.alert').remove();
$('.container.contents').prepend('<div class="alert alert-info">Book vented from airlock.</div>');
$('#cart_link').html('Cart: ( <%= current_cart.book_count %> Books | $<%= current_cart.price_total %> )');
$('#cart-total').text('<%= current_cart.price_total %>');
